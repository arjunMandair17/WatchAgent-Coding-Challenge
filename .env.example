
# --- PostgreSQL (postgres service) ---
POSTGRES_DB=weather
POSTGRES_USER=weather_app

# Optional. Empty for the default docker-compose trust setup.
POSTGRES_PASSWORD=

# Host for tools on your machine (uvicorn, pytest) reaching published port 5432.
# Inside Compose, the api service overrides this to the hostname "postgres".
POSTGRES_HOST=localhost