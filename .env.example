# Copy to .env and set POSTGRES_PASSWORD before starting:
#   cp .env.example .env
#
# Do not commit .env — it is listed in .gitignore.

POSTGRES_DB=weather
POSTGRES_USER=weather_app
POSTGRES_PASSWORD=

# Host for local API/tests against Docker Postgres (Compose uses service name "postgres").
POSTGRES_HOST=localhost
